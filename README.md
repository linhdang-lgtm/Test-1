# Test-1

Simple inventory web app for a racket sport equipment company, backed by a local SQLite database seeded with 20 products.

## Run

```bash
python app.py
```

Then open `http://127.0.0.1:8000`.

## Test

```bash
python -m unittest discover -s tests -v
```
